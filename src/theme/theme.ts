"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#e9ecef",
      main: "#070D14",
      dark: "#1D2329",
    },
    secondary: {
      main: "#F9BA48",
    },
  },
});

export default theme;
